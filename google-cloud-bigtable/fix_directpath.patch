--- sidecar/src/main/java/com/example/sidecar/SidecarServiceImpl.java
+++ sidecar/src/main/java/com/example/sidecar/SidecarServiceImpl.java
@@ -3,6 +3,8 @@
 import com.google.cloud.bigtable.data.v2.BigtableDataClient;
 import com.google.cloud.bigtable.data.v2.BigtableDataSettings;
 import com.google.cloud.bigtable.data.v2.models.Query;
+import com.google.api.gax.grpc.InstantiatingGrpcChannelProvider;
 import com.google.cloud.bigtable.data.v2.models.Row;
 import com.google.protobuf.ByteString;
 import io.grpc.stub.StreamObserver;
@@ -60,6 +62,13 @@
             // Use the standard endpoint, the library will handle the upgrade to DirectPath
             settingsBuilder.stubSettings().setEndpoint("bigtable.googleapis.com:443");
             
+            InstantiatingGrpcChannelProvider channelProvider =
+                ((InstantiatingGrpcChannelProvider) settingsBuilder.stubSettings().getTransportChannelProvider())
+                    .toBuilder()
+                    .setAttemptDirectPath(true)
+                    .build();
+            settingsBuilder.stubSettings().setTransportChannelProvider(channelProvider);
+            
             // NOTE: We don't need to manually configure InstantiatingGrpcChannelProvider here
             // BigtableDataSettings properly configures DirectPath if CBT_ENABLE_DIRECTPATH=true is set in env
